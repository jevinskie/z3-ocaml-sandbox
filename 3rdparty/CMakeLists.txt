include(gmp.cmake)
include(fmt.cmake)
include(boost.cmake)
include(folly.cmake)
include(sha2.cmake)
include(thread-pool.cmake)
include(z3.cmake)

add_subdirectory(indicators)

if(JEV_USE_MIMALLOC)
    set(MI_USE_CXX OFF)
    set(MI_NO_USE_CXX ON)
    set(MI_OSX_ZONE OFF)
    set(MI_OSX_INTERPOSE OFF)
    set(MI_OVERRIDE OFF)
    set(MI_BUILD_SHARED OFF)
    set(MI_BUILD_STATIC OFF)
    set(MI_BUILD_OBJECT ON)
    set(MI_BUILD_TESTS OFF)
    set(MI_XMALLOC ON)
    add_subdirectory(mimalloc)
endif()
